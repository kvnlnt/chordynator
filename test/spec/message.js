/* global describe, it */

(function () {
    'use strict';

    describe('Message Model', function () {
        describe('validate defaults', function () {
            it('defaults should be', function () {
                assert(1, 1);
            });
        });
    });
})();
